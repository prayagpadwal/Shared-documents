-- Example for 'Initiation' phase
ROUND(CASE WHEN dot.[PDC Program] = 'ED OH Conductor Repl Non-HFTD' THEN dot.[Initiation Target]
           ELSE ROUND(dot.[Initiation Target], 0)
      END, 4) AS [Plan],
